# 使用说明

> 这份是写给**用这个站的人**的：怎么打开、怎么改、怎么备份、遇到问题怎么办。
> 想知道代码怎么写的、参考了哪些开源项目、素材的许可边界在哪，看 [README.md](README.md)；
> 第三方素材的逐项说明在 [THIRD_PARTY_ASSETS.md](THIRD_PARTY_ASSETS.md)。

---

## 一分钟了解

**元素手帐**是一个个人手帐站：首页全屏壁纸 + 一段滚动叙事的「记忆」、手记列表、
作品集、项目看板、课表、游乐场（四个能真的玩的小游戏）、自述、友链，
还有一个局域网聊天室「茶室」。

整站最大的特点是 **所见即可改**：凡是鼠标移上去会浮出 ✎ 的文字，点一下就能改，
改完就存在你自己的机器上（不需要登录、不联网）。图片也能换。

它有两种形态，用的是同一套网页文件：

| | 网页版 | 桌面版 |
|---|---|---|
| 怎么跑 | 浏览器打开 | Electron 应用（安装包 / 免安装） |
| 数据存在哪 | 浏览器本地存储 | 磁盘文件 `%APPDATA%\elemental-journal\data\` |
| 适合 | 托管到网上、给别人看 | 自己日常记录、离线用 |

---

## 一、三种打开方式

### 1. 桌面版（推荐日常用）

- **安装版**：双击 `元素手帐-1.0.0-安装包.exe`。
  安装过程会问你三件事：**装到哪个目录**、**要不要建快捷方式**（桌面 / 开始菜单两个勾选框，
  默认都勾上，不想要就取消）、**装完要不要直接启动**。
- **免安装版**：直接双击 `免安装版\元素手帐.exe`。整个文件夹是完整的，
  可以拷到 U 盘或另一台 Windows 上用。

> 安装包没有买代码签名，Windows SmartScreen 可能提示「未知发布者」——
> 点「更多信息」→「仍要运行」即可。

### 2. 网页版（最省事）

- 想本机看：仓库根目录执行 `node server.js`，然后打开 `http://127.0.0.1:5210/`。
  （也可以直接双击 `index.html`，但聊天室会降级成"同一浏览器多标签"那一档。）
- 想让同一个 WiFi 下的手机 / 平板也能进聊天室：`MJ_HOST=0.0.0.0 node server.js`，
  把窗口里打印的地址分享出去。**公共 WiFi 下别这么干。**

### 3. 自己托管一份（GitHub Pages / Gitee Pages / 任意静态托管）

这个站是**零构建**的：没有打包步骤，把仓库根目录的文件原样传上去就能跑。

```
index.html  enhance.css  enhance.js  modules.css  modules.js
chat.css  chat.js  qr.js  assets/  …
```

GitHub Pages：仓库 Settings → Pages → Deploy from a branch → 选 `main` / `/ (root)`。
Gitee Pages 同理（在「服务 → Gitee Pages」里开）。

> 只想要站点、不需要桌面壳的话，`electron/`、`server.js`、`chatd.js`、`package.json`
> 这几个可以不上传 —— 去掉它们不影响浏览，只少了"局域网聊天"和"桌面窗口"。

---

## 二、怎么改内容

整站的设计是「所见即可改」：**鼠标移上去浮出 ✎ 的文字，点一下就能改**，
按 Enter 或点别处保存，存在本地。

| 位置 | 能改什么 |
|---|---|
| 首页主 / 副标题 | 大标题、两行副标题 |
| 左侧「个人资料」 | 头像（点圆点换）、名字、签名、下面那排平台标签（名称 + 链接，可增删） |
| 左栏「公告」「标签」 | 每条都能改，能 ＋ / × |
| 右栏「目录」「分类」 | 名称与计数都能改 |
| 首页第二幕「记忆」 | 鼠标移上去浮出换图条，会写明"正在改第几幕、照片墙哪几张跟着换" |
| 关于页 | 身份档案 / 时间线 / 兴趣 / 在玩的游戏 / 本站的技术栈 / 近况，全部可增删改，兴趣卡还能换图 |
| 作品集 | 封面、标题、类型、时间、简介，可增删、可按类型筛 |
| 项目 | 每个项目由**文字 / 图片 / 视频**块组成，块可加、删、上下移动 |
| 友链 | 头像（点头像换图，换完可用 ↺ 换回色块）、名称 / 简介 / 链接，可增删 |
| 茶室 | 昵称、头像、表情包 |

**换掉占位素材**：站里的背景图、头像都是占位用的 —— 「关于」页的头像框、
首页第二幕的换图控件，点一下就能换成你自己的图（自动缩小后存到本地）。

**改坏了想还原**：「关于」页右上角有「恢复默认」；其它区块把内容删空也会回到默认。

### 外观

右下角齿轮里能调：

- **壁纸模式**：横幅 / 全屏 / 透明 / 无
- **主题色相**：滑一下整站配色跟着走（七个元素的色系都在这条色相环上）
- **壁纸透明度 / 卡片透明度 / 卡片描边**
- **壁纸柔化**（默认**关**）：给全屏壁纸加一层 1px 模糊。观感差别很轻微，
  但代价不小：全屏图上的滤镜会让这一层多走一遍光栅化，实测帧时间中位数从 4.2ms 涨到 12.4ms。
- **氛围特效**：樱花 / 萤火 / 弹幕 / 涟漪

---

## 三、数据在哪、怎么备份

| | 位置 | 怎么备份 |
|---|---|---|
| 桌面版 | `%APPDATA%\elemental-journal\data\`（`store.json` 站点数据、`chat.json` 房间码、`chat-history.json` 聊天记录、`chat-blobs\` 图片） | 设置面板里的「导出」会存成一个 JSON；也可以直接拷这个文件夹 |
| 网页版 | 浏览器的本地存储（localStorage） | 用设置面板的「导出」存 JSON；换浏览器 / 清缓存会丢，记得导出 |

- **卸载桌面版不会删你的数据**（安装时就没勾"卸载时删除应用数据"）。
- 导入导出按钮在右下角齿轮面板里（`导出` / `导入`）。

---

## 四、茶室（局域网聊天室）

- **怎么用**：一台机器起来当服务端（桌面版：菜单栏「茶室」；网页版：`MJ_HOST=0.0.0.0 node server.js`），
  别人用同一个 WiFi 打开你分享的地址，用**房间码**进同一个房间。
- **房间码**：桌面版在「茶室」菜单里看，网页版在服务端窗口里。想换一个也在菜单里。
- **头像 / 表情包**：点自己那个圆点换头像（自动缩到 256px，别人立刻能看到）；
  输入框左边的 ☺ 里能导入表情包和动图（动图原样发，不会被压成静图）。
- **两条通道自动切换**：有服务端就走真局域网（SSE 推送）；没有（比如直接双击 `index.html`）
  就退化成"同一浏览器多标签"，适合自己试。
- 断线会自动重连并补拉漏掉的消息。

---

## 五、键盘 / 鼠标小抄

| 操作 | 作用 |
|---|---|
| `Ctrl + K` | 打开顶栏检索（回车用百度搜，`Esc` 关闭） |
| `Ctrl + Shift + F` | 显示 / 隐藏帧率表 |
| `Esc` | 关闭当前浮层 / 取消正在编辑的文字 |
| 小游戏 | 贪吃蛇：方向键或 WASD 转向、空格暂停；俄罗斯方块：← → 移动、↑ 旋转、↓ 软降、空格直落、`P` 暂停；打砖块：← → 或直接拖鼠标控制挡板、空格发球 / 暂停 |

---

## 六、常见问题

**装的时候提示"未知发布者"？** 安装包没买代码签名，选「仍要运行」即可。

**桌面图标不见了，或者装的时候没勾？** 到安装目录里把 `元素手帐.exe` 自己发一个快捷方式到桌面；
或者重新运行一次安装包，在「快捷方式」那一页把勾打上。

**窗口拉窄以后，首页第二幕不动了？** 这一段是**滚动叙事**（钉住一屏，靠滚动推进 5 幕 + 揭开照片墙），
需要足够的滚动距离。窗口窄于 720px 时它会自动收成一张静态画面（进度条和照片墙一并收起，
提示会写明原因）。**把窗口拉到 768px 以上就恢复正常**。这个站是按桌面尺寸设计的。

**帧率怎么看？** 右下角状态栏一直显示 fps；`Ctrl+Shift+F` 会弹出更详细的表。
想看数字对不对，可以对照 README 里的性能标定。

**桌面版刚启动的头几秒有点卡？** 那是 Chromium 的 GPU 进程还在就绪中（整页暂时是软件光栅化），
不是站点的问题，几秒后就好。

**聊天室别人进不来？** 三个常见原因：① 服务端没绑到内网地址（要用 `MJ_HOST=0.0.0.0`）；
② Windows 防火墙拦了 —— 首次运行会弹窗，要允许**专用网络**；③ 不在同一个 WiFi / 路由器下。

**项目正文里的视频不能离线播？** 视频块内嵌的是 B 站 / YouTube 的外部播放器，需要联网。
想离线看就把那块改成选本地视频文件。

**想换成自己的图，但站里有别人的素材吗？** 站里的背景图与头像都是占位素材，
换成你自己的最省事；素材的来源与许可边界见 [THIRD_PARTY_ASSETS.md](THIRD_PARTY_ASSETS.md)。

---

## 七、从源码跑起来（想改代码再看）

```bash
# 网页版：零构建，直接用 Node 起个静态服务
node server.js                # 默认 http://127.0.0.1:5210/
MJ_HOST=0.0.0.0 node server.js   # 让局域网也能访问（含茶室）

# 桌面版
npm install                   # 只为 Electron 壳，站点本身零依赖
npx electron .

# 打 Windows 安装包（输出到 dist-app/）
npm run dist
```

打包相关的两个文件：

- `package.json` 里的 `build.nsis` 段：安装器选项（可改安装目录、快捷方式默认勾选）
- `build/installer.nsh`：安装器的**自定义页** —— 就是问"要不要建快捷方式"那一页。
  用的是 electron-builder 的 `customPageAfterChangeDir` / `customInstall` 两个宏钩子；
  文件必须是 **UTF-8 带 BOM**（否则 NSIS 按系统 ANSI 读，中文会乱码），
  改完直接 `npm run dist` 即可。
