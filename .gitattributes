# 统一行尾，避免不同系统上 diff 出整文件差异
* text=auto eol=lf

# Windows 批处理必须是 CRLF —— 纯 LF 下 cmd.exe 解析 goto 标签会出问题
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制资源不要做任何行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ico binary
*.woff2 binary
